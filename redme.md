Aplikacja symulująca działanie kont bakowych.
Umożliwia wykonywanie wpłat, wypłat oraz przelewów.
Posiada dwa typy konta - osobiste oraz firmowe(z opcją debetu).

Wszystkie operacje wykonują się wewnątrz skryptu bez interakcji z użytkownikiem.

Uruchomienie w konsoli za pomocą:
php index.php